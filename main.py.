import asyncio
from aiogram import Bot, Dispatcher, types
from aiogram.filters import Command

# Pega tu token aquí abajo, entre las comillas
TOKEN_TELEGRAM = '8441666201:AAHygO1Osx5IdxnmQpQuF__Y8WyGvBKhr4U' 

bot = Bot(token=TOKEN_TELEGRAM)
dp = Dispatcher()

@dp.message(Command("start"))
async def bienvenido(message: types.Message):
    await message.answer(f"¡Hola! 👋 Bot conectado correctamente.")

@dp.message()
async def capturar(message: types.Message):
    if message.text and "amazon" in message.text.lower():
        await message.answer("✅ Enlace de Amazon detectado.")
    else:
        await message.answer("Pásame un link de Amazon para probar.")

async def main():
    await bot.delete_webhook(drop_pending_updates=True)
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())
